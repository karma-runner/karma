// files to load
files = [
  JASMINE,
  JASMINE_ADAPTER,
  '*.js'
];

autoWatch = true;
autoWatchInterval = 1;
logLevel = LOG_INFO;
logColors = true;

browsers = ['Chrome'];

plugins = [
  'testacular-chrome-launcher',
  'testacular-firefox-launcher'
];
